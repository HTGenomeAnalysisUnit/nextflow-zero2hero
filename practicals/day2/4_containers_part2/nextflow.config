singularity {
    enabled = true
    autoMounts = true
    // Optionally add extra Singularity CLI options (see "Traps" section)
    // runOptions = '--bind /host/path:/container/path'
}

params {
    input_file = "assets/input.txt"
}